# Só o que o servidor MCP precisa em produção.
# A coleta (playwright, httpx, beautifulsoup4, lxml) roda na sua máquina e não
# vai para a imagem: ela apenas serve o acervo já coletado.
#
# Este arquivo é a ÚNICA declaração da dependência: o empacotador .mcpb lê
# daqui (`_exigencias()`), em vez de repetir o pin.
# `mcp` pinado abaixo de 2: a 2.x removeu `mcp.server.fastmcp`, que é o que
# `legis/servidor.py` importa.
mcp>=1.28,<2
